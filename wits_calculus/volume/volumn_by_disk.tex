%\documentclass{article}
\renewcommand{\name}{ \section*{Volumn slicing by disk} }
\renewcommand{\definition}{}
 \renewcommand{\result}{
		\paragraph{Result}
	\begin{paragraph}
\noindent
	The volumn obtained by rotating the area between $f(x)$ and $g(x)$ over the interval $[a,b]$ around the $x$ axis is 
	$$ V = \pi \int_a^b f(x)^2-g(x)^2$$

\end{paragraph}
}
%\writeall
