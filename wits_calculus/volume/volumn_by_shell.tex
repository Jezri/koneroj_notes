%\documentclass{article}
\renewcommand{\name}{ \section*{Volumn by shell} }
\renewcommand{\definition}{
 }
 \renewcommand{\result}{
		\paragraph{Result}
	\begin{paragraph}
	\noindent
The volumn that the area under a $f(x)$ will sweep through when rotated $\theta$ radians around the axis $x = L$ is given by
$$V = \int_a^b 2\pi (x-L)(f(x))dx$$
\end{paragraph}
}

%\writeall
