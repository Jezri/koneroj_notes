%\documentclass{article}
\renewcommand{\name}{ \section*{Volumn} }
\renewcommand{\definition}{
		\paragraph{Definition}{
	\begin{paragraph}
	\noindent
	The volumn of a solid with integrable cross-sectional area $A(x)$ over the closed interval $[a,b]$ is
	$$ V = \int_a^b A(x)dx$$
\end{paragraph}
}
 }
 \renewcommand{\result}{}
%\writeall
