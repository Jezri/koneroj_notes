%\documentclass{article}
\renewcommand{\definition}{}
\renewcommand{\name}{ \section*{Max min inequality} }
\renewcommand{\result}{
	\paragraph{Result}
	\begin{paragraph}
	\noindent
	If $f$ has maximum value $f_{\text{max}}$ and minimium value $f_{\text{min}}$
	$$ f_min(b-a)\leq \int_a^b f(x)dx \leq f_max(b-a) $$
	\end{paragraph}
}
%\writeall
