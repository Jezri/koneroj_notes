\renewcommand{\name}{ Max min inequality }
\renewcommand{\result}{ 
	If $f$ has maximum value $f_{\text{max}}$ and minimium value $f_{\text{min}}$
	$$ f_min(b-a)\leq \int_a^b f(x)dx \leq f_max(b-a) $$
}
