%\documentclass{article}
\renewcommand{\definition}{}
\renewcommand{\name}{ \section*{Zero width interval} }
\renewcommand{\result}{
	\paragraph{Result}
	\begin{paragraph}
	\noindent
	$ \int_{a}^{b}f(x)dx \dependency{Intergal}{=} - \int_{b}^{a}f(x)dx \quad$ where $f$ is integratable of interval$[a,b]$ 
	\end{paragraph}
}
%\writeall
