%\documentclass{article}
\renewcommand{\definition}{}
\renewcommand{\name}{ \section*{Additivity} }
\renewcommand{\result}{
	\paragraph{Result}
	\begin{paragraph}
	\noindent
	$\int_a^b f(x) dx + \int_b^c f(x)dx = \int_a^c f(x)dx$
	\end{paragraph}
}
%\writeall
