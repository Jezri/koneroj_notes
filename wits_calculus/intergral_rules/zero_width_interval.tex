
%\documentclass{article}
\renewcommand{\definition}{}
\renewcommand{\name}{ \section*{Zero width interval} }
\renewcommand{\result}{
	\paragraph{Result}
	\begin{paragraph}
	\noindent
	$ \int_{a}^{a}f(x)dx \dependency{Intergal}{=} 0 \quad$ where $f$ is integratable of interval$[a,b]$ 
	\end{paragraph}
}
%\writeall
