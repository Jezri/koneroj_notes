\renewcommand{\name}{\section*{Partition subinterval} }
\renewcommand{\definition}{
	\paragraph{Definition}
	\begin{paragraph}
	\noindent
	A sub interval of a partition(  $x_0 \text{ to } x_n$ ) is $x_{k+1} -x_k$
 	\end{paragraph}
 }
