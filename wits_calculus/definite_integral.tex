%\documentclass{article}
%\documentclass{standalone}
%\usepackage{koneroj}
%\begin{document}
\renewcommand{\name}{\section*{Definate intergral} }
\renewcommand{\definition}{
	\paragraph{Definition}
	\begin{paragraph}
	\noindent
	The definate intergral of a function $f$ over a closed interval $[a,b]$ is:
	$$ \lim_{||P|| \to 0} \sum_{k=0}^{n} f(c_k)\Delta_k $$ 
	where:
	$ \sum_{k=0}^{n} f(c_k)\Delta_k $ is any reimmann sum of $f$ on interval $[a,b]$ 
	\\ It is donaoted by
	$$\int_{a}^{b} f(x) dx$$
	\end{paragraph}
}
%\writeall
%\end{document}
