%\documentclass{article}
\renewcommand{\name}{ \section*{Mean value theorem for definite intergrals} }
\renewcommand{\definition}{}
\renewcommand{\result}{
	\begin{paragraph}
	\noindent
 	if $f$ is continues over $[a,b]$ than at some point $c$ in $[a,b$$$f(c)=\frac{1}{b-a}\int_a^bf(x)dx$$
	\end{paragraph}
 }
%\writeall
