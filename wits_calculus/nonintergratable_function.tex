%\documentclass{article}
%\documentclass{standalone}
%\usepackage{koneroj}
%\begin{document}
\renewcommand{\name}{\section*{Norm of a partition} }
\renewcommand{\definition}{}
\renewcommand{\result}{
\paragraph{Result}
	\begin{paragraph}
	\noindent
	If two rieman sums of a function $f$ over and closed interval $[a,b] $ are not equal, $f$ is not intergratable over $[a,b]$ 

	\end{paragraph}
}
%\writeall
%\end{document}
